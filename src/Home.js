import { View, Text } from "react-native";
import React, { useState, useEffect, useRef, useContext } from "react";

const Home = () => {
  // COMPONENT VARIABLES
  // CAMERA VARIABLES
  // DARK MODE VARIABLES ?????
  return (
    <View>
      <Text>Home</Text>
    </View>
  );
};

export default Home;
